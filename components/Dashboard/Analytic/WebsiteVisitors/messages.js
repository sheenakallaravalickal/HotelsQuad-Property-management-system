/*
 * WebsiteVisitors Messages
 *
 * This contains all the text for the WebsiteVisitors component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WebsiteVisitors';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WebsiteVisitors component!',
  },
});
